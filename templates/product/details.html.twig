{% extends "base.html.twig" %}

{% block body %}
    <div class="card" style="width: 18rem;">
        <img class="card-img-top" src="{{ test.picture }}" alt="Card image cap">
        <div class="card-body">
            <h5 class="card-title">{{ test.name }}</h5>
            <p class="card-text">{{ test.price }}€</p>
        </div>
        </div>
{% endblock %}